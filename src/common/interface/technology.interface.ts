/**
 * Data model for a specific technology image
 *
 * @export interface
 * @interface Technology
 */
export interface Technology {
  id: number;
  name: string;
  image: string;
  primaryColor: string;
  secondaryColor: string;
}
