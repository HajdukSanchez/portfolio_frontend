/**
 * Data model for create a Home section
 *
 * @interface HomeSectionProps
 */
export interface HomeSectionProps {
  id: number;
	idSection: string;
  title: string;
  image: string;
  description: string;
  buttonText: string;
  buttonLink: string;
}
