/**
 * Enum for the routes to be centralized
 *
 * @export enum
 * @enum {number}
 */
export enum RoutesNavigation {
  Home = '/',
  AboutMe = '/about-me',
  Project = '/project',
  Projects = '/projects',
  Certificate = '/certificate',
  Certificates = '/certificates',
}
